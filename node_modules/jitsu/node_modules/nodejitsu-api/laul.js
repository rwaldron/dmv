var request = require('request');

request.post({url: 'http://localhost:8080/api', json: {user: 'rastabob'}}, function (e, r, b) { 
  if (e) console.dir(e); 
  if (r) console.dir(r);
  if (b) console.log(b.toString());
});


